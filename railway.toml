[build]
builder = "nixpacks"

[deploy]
startCommand = "python scripts/seed_demo.py && uvicorn api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
